"use client";


export function RecentActivityTicker() {
  // Temporary: Returning null to hide mock data. In the future, this should be connected to a real websocket or polling endpoint.
  return null;
}
